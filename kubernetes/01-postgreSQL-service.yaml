apiVersion: v1
kind: Service
metadata:
  name: psql-service
  labels:
    group: nighty.night.db
    app: PostgreSQL
    branch: minikube
spec:
  #type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      #nodePort: 31000
  selector:
    app: PostgreSQL